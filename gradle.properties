org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
org.gradle.warning.mode=all

# Локальнее и тише даггеров: отключить параллельный конфиг по желанию
# org.gradle.configuration-cache=false

# Fabric Loom
loom.platform=forge
# ↑ НЕ ТРОГАТЬ. Для Fabric с Loom 1.6 это поле игнорируется, но если паранойя — просто удали строку.
